A tiny mixin for React that handles implemeting seamless navigation using HTML5 pushState.

Installation:

    npm install --save reactor-pushstate

Usage:

    var PushState = require('reactor-pushtate');

    // ... in your react createClass()
    mixins: [ PushState ];